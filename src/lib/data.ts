
import type { Vehicle } from './types';

// This file is now empty as vehicle data is fetched from a remote API.
// The empty `vehicles` export is kept to prevent breaking imports in other files
// that are also being updated in this change.
export const vehicles: Vehicle[] = [];
